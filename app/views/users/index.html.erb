<% provide :title, 'All users' %>

<div class="page-header">
  <h1>All users</h1>
</div>

<%= paginate @users %>

<div class="row-fluid">
  <div class="span6 offset6">
    <ul class="users-list">
      <%= render @users %>
    </ul>
  </div>
</div>

<%= paginate @users %>
